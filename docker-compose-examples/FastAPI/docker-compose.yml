version: "3.9"
services:
  nginx:
    container_name: nginx
    restart: always
    image: nginx:latest
    volumes:
      - ./templates:/etc/nginx/templates
    ports:
      - "8080:80"

    environment:
      - NGINX_HOST=foobar.com
      - NGINX_PORT=80